const fs=require('fs'); const files = ['dummy/inventory.ts', 'dummy/stores.ts', 'dummy/chat.ts']; for(let f of files) { let c = fs.readFileSync(f, 'utf8'); let n = 0; c = c.replace(/https:\/\/images\.unsplash\.com\/photo-[^?"']+(\?[^"']*)?/g, (match) => { n++; let w = match.includes('w=96') ? 96 : 400; return 'https://picsum.photos/seed/' + Math.random().toString(36).substring(7) + '/' + w; }); fs.writeFileSync(f, c); } console.log('Done');
